<% include ./partials/header %>

<div class="ui main text container segment">
    <form class="ui form" action=/bowlers/ method="POST">
        <div class="field">
            <h1>New Bowler</h1>
            <label>Name</label>
            <input type="text" name="bowler[bowler]" placeholder="Username">
        </div>
        <div class="field">
            <label>Strikes missed</label>
            <input type="number" name="bowler[missedStrikes]" placeholder="Strikes missed">
        </div>
        <div class="field">
        </div>
        <button class="ui inverted violet button" type="submit">Submit</button>
    </form>
</div>

<% include ./partials/footer %>