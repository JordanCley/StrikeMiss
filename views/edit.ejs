<% include ./partials/header %>

<div class="ui main text container segment">
    <form class="ui form" action="/bowlers/<%= bowler._id %>?_method=PUT" method="POST">
        <div class="field">
            <h1>Edit Bowler</h1>
            <label>Bowler</label>
            <input type="text" name="bowler[bowler]" value="<%= bowler.bowler %>">
        </div>
        <div class="field">
        </div>
        <button class="ui inverted violet button" type="submit">Submit</button>
    </form>
</div>

<% include ./partials/footer %>